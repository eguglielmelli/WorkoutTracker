package com.eguglielmelli.entities;

/**
 * Enum to unify the type of the workout instead of having to enter
 * each workout type as a string
 */
public enum WorkoutType {
    RUNNING, WEIGHTLIFTING, CYCLING, SWIMMING, HIIT,
    WALKING, ROWING
}
